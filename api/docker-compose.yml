# Local development compose: MongoDB only.
# The API runs outside the container (uvicorn --reload) for reactive reloading.
services:
  mongo:
    image: mongo:8.0
    container_name: MongoDB
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
