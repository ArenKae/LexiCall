<Window x:Class="LexiCall.Desktop.CategoriesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gérer les catégories"
        Height="520" Width="760"
        MinHeight="460" MinWidth="680"
        WindowStartupLocation="CenterOwner"
        Background="#F7F7FA"
        FontFamily="Segoe UI">
    <!--
        CRUD minimal des catégories. Les changements restent locaux à cette
        fenêtre jusqu'au clic sur Valider.
    -->
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Gérer les catégories"
                       FontSize="26"
                       FontWeight="SemiBold" />
            <TextBlock Text="Les catégories sont optionnelles pour les mots, mais utiles pour trier et retrouver ton vocabulaire."
                       Margin="0,4,0,0"
                       Foreground="#666676" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,20,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ListBox Grid.Column="0"
                     ItemsSource="{Binding Categories}"
                     SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
                     Background="White"
                     BorderBrush="#DDDDE5"
                     BorderThickness="1"
                     Padding="4">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="10" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}"
                                       FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Description}"
                                       Margin="0,4,0,0"
                                       Foreground="#666676"
                                       TextWrapping="Wrap"
                                       MaxHeight="42" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Grid.Column="2"
                    Padding="20"
                    Background="White"
                    BorderBrush="#DDDDE5"
                    BorderThickness="1"
                    CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Catégorie"
                               FontSize="18"
                               FontWeight="SemiBold" />

                    <TextBlock Text="Nom *"
                               Margin="0,18,0,0"
                               FontWeight="SemiBold" />
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,6,0,0"
                             Padding="10,7" />

                    <TextBlock Text="Description"
                               Margin="0,16,0,0"
                               FontWeight="SemiBold" />
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,6,0,0"
                             Height="88"
                             Padding="10,7"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" />

                    <TextBlock Text="{Binding ErrorMessage}"
                               Margin="0,14,0,0"
                               Foreground="#B42318"
                               FontWeight="SemiBold" />

                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="Valider enregistre les changements."
                       Margin="0,0,16,0"
                       VerticalAlignment="Center"
                       Foreground="#666676"
                       TextTrimming="CharacterEllipsis" />

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Nouvelle"
                        Width="96"
                        Padding="12,8"
                        Margin="0,0,10,0"
                        Click="NewCategoryButton_Click" />
                <Button Content="Supprimer"
                        Width="96"
                        Padding="12,8"
                        Margin="0,0,10,0"
                        Click="DeleteCategoryButton_Click" />
                <Button Content="Annuler"
                        Width="96"
                        Padding="12,8"
                        Margin="0,0,10,0"
                        IsCancel="True" />
                <Button Content="Valider"
                        Width="96"
                        Padding="12,8"
                        IsDefault="True"
                        Click="AcceptButton_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
