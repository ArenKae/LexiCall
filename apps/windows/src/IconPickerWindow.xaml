<Window x:Class="LexiCall.Desktop.IconPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:LexiCall.Desktop.ViewModels"
        Title="Choisir une icône"
        Height="580" Width="460"
        MinHeight="420" MinWidth="380"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Window.Background}"
        Foreground="{DynamicResource Brush.Text.Primary}"
        FontFamily="Segoe UI">
    <!-- Icon picker: themed collections (CategoryIconCatalog) filterable by
         keyword. Clicking an icon selects it immediately (no separate
         confirm button). -->
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Choisir une icône"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12" />

        <TextBox Grid.Row="1"
                 x:Name="SearchTextBox"
                 Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,14"
                 ToolTip="Filtrer les icônes par mot-clé (ex. « mer », « coeur », « livre »)." />

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Groups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type viewModels:IconPickerGroupViewModel}">
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="{Binding Name}"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Margin="0,0,0,8" />
                            <ItemsControl ItemsSource="{Binding Icons}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Width="44" Height="44"
                                                Margin="0,0,6,6"
                                                Padding="0"
                                                Tag="{Binding Glyph}"
                                                ToolTip="{Binding Keywords}"
                                                Click="IconButton_Click">
                                            <TextBlock Text="{Binding Glyph}"
                                                       FontFamily="Segoe UI Emoji, Segoe UI Symbol, Segoe UI"
                                                       FontSize="20" />
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
