<Window x:Class="LexiCall.Desktop.Windows.ImagePreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aperçu de l'image"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource Brush.Window.Background}"
        Foreground="{DynamicResource Brush.Text.Primary}"
        FontFamily="Segoe UI"
        KeyDown="Window_KeyDown">
    <!-- Enlarged image preview -->
    <StackPanel Margin="24">
        <Grid>
            <Image x:Name="PreviewImage"
                   Stretch="Uniform"
                   Cursor="Hand"
                   MouseLeftButtonUp="PreviewImage_MouseLeftButtonUp" />

            <Button x:Name="PreviousButton"
                    Width="44" Height="44"
                    Padding="0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Margin="8,0,0,0"
                    ToolTip="Image précédente"
                    Click="PreviousButton_Click">
                <Path Data="M 8 2 L 2 9 L 8 16"
                      Stroke="{DynamicResource Brush.Text.Primary}"
                      StrokeThickness="2"
                      StrokeLineJoin="Round"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"
                      Width="13" Height="20"
                      Stretch="Uniform" />
            </Button>

            <Button x:Name="NextButton"
                    Width="44" Height="44"
                    Padding="0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="0,0,8,0"
                    ToolTip="Image suivante"
                    Click="NextButton_Click">
                <Path Data="M 2 2 L 8 9 L 2 16"
                      Stroke="{DynamicResource Brush.Text.Primary}"
                      StrokeThickness="2"
                      StrokeLineJoin="Round"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"
                      Width="13" Height="20"
                      Stretch="Uniform" />
            </Button>
        </Grid>

        <TextBlock x:Name="CaptionText"
                   Margin="0,10,0,0"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   Foreground="{DynamicResource Brush.Text.Secondary}" />

        <Button Content="Fermer"
                Width="96"
                Margin="0,16,0,0"
                HorizontalAlignment="Right"
                IsCancel="True" />
    </StackPanel>
</Window>
