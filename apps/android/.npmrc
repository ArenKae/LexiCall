# Expo SDK 57's bundled web tooling (@expo/ui, vaul, radix-ui) pulls in a react-dom
# peer range that doesn't match Expo's own pinned react version; harmless since this
# project never targets the web platform, but npm's strict resolver blocks on it otherwise.
legacy-peer-deps=true
